# PLAYBOOK

方法手冊。只寫做法的形狀,不放任何頻道內部數字(觀看、收益、訂閱、配額)。

## 1. 定位

替一群具體的人解決具體的問題;題目要是他們會**打進搜尋框**的問題。搜尋流量穩定、長尾;推薦流量看運氣。

## 2. 格式

長片優先。Shorts 帶來觀看次數,但很少轉成訂閱,也不計入 YPP 的公開觀看時數。比較格式時用「觀看分鐘」當單位,不要用觀看次數。

## 3. 選題與題庫

題庫會被消耗 —— 腳本被閘門擋下重寫時,每次重寫都在吃題目。要有去重紀錄(本 repo 的 `output/topics_used.json`),並定期補種子主題;產量掉下來先查題庫是不是空了,不要先怪閘門。

## 4. 腳本結構

開場用完整句子直接點出痛點(碎句開場留不住人);每段結尾一句收束句說清楚結論;結尾邀請訂閱並留下一集的懸念。系列節目不要每集都「打臉」同一種套路,觀眾會膩。

## 5. 誠信閘門

AI 會編數字,而且編得很像。只檢查「這個數字有沒有出處」不夠 —— 真實數字掛到錯的主體上一樣是造假,單位不同也是造假(12.5% 不能拿來背書 12.5 倍),所以本 repo 的閘門查「數字、單位、主體都要跟同一筆 fact 對上,而且主體要出現在同一句」;句中如果同時出現另一個「不擁有此數字」的已知主體,歸屬有歧義,一樣擋。寫在 prompt 裡的規則不會被執行,要擋就寫成程式碼。標題和說明欄也是暴露面,一樣要檢查。

**閘門查得到的範圍是有限的**,以下都擋不到,發布前一定要人工看過:
1. 中文數字(「三成」「十二億」這類)——閘門只認阿拉伯數字。
2. 沒登記在 `facts` 裡的新主體。例如「比起甲公司,乙公司毛利率高達12.5%」,如果乙不在 `facts` 清單裡,閘門看不出乙是另一個主體,擋不下來。歸屬歧義的判斷只對「facts 裡列出的主體」有效。
3. 年份豁免:任何 `19xx`/`20xx` 後面接「年」一律當年份放行,不檢查它是不是真的年份、也不檢查有沒有對應的 fact。這是刻意留下的已知漏洞,沒有修。
4. 正負號只認緊接數字前面的 `-` 或 `−`。「10-20元」這種當範圍用的連字號,會被誤判成負的 20。

## 6. 發布節奏與判讀

這裡有兩個數字,分別回答兩個不同的問題,不要混用:

- **近 2 天增量**:回答「現在要不要推這支片」。新片的流量集中在發布後頭幾天,之後很快衰減,判斷要不要現在推,看的是最近兩天的增量,不要看累計。YouTube Analytics 有幾天延遲,拿「到今天為止」做週對週比較會製造假警報。
- **`pipeline.stats` 的「平均每日分鐘」(`minutes_per_day`)**:回答「哪個題目值得再做一支」,回饋給下一次選題。片齡不同,直接比累計觀看分鐘對新片不公平(舊片有更多天數可以累積),所以 `python -m pipeline.stats` 排的是「窗口內平均每天觀看分鐘」,不是累計 minutes,也不是觀看次數。

## 7. 常見坑

配音檔邊產邊寫在最終路徑 → 中斷時留下半截檔還被當成完成(本 repo 用 tmp→replace);API 配額用完時錯誤被 `except` 吞掉 → 靜默產出壞資料(不要吞例外);同一題派兩次工 → 互相覆蓋(本 repo 每次一個時間戳目錄,片子本身不會撞名)。但**不要同時跑兩個 `pipeline.run` 行程**——時間戳目錄只隔開片子的檔案,`output/topics_used.json` 和 `output/published.json` 是全域共用的檔案,兩個行程同時寫會互相蓋掉對方剛寫入的紀錄。

## 8. 合規紅線

1. YouTube「重複性/量產內容」政策 —— 全自動、範本化、缺少人的價值會被排除在營利之外,自動化只該拿來省力,不能取代你的判斷。
2. 不要爬 YouTube 網站(服務條款禁止 scraping),只用官方 API。
3. 不要買觀看、刷觀看,會連帶傷害頻道,也會讓你自己的數據失真。
